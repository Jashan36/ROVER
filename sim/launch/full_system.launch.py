"""Full system launch: Gazebo + robot_state_publisher + Nav2 + AI bridge (stub)"""
from launch import LaunchDescription

def generate_launch_description():
    ld = LaunchDescription()
    # Compose full system here
    return ld
