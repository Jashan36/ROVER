"""ROS2 Python launch stub to start Gazebo/ignition with the rover and world."""
from launch import LaunchDescription

def generate_launch_description():
    ld = LaunchDescription()
    # Add nodes/launch actions here (gazebo, robot_state_publisher, etc.)
    return ld
